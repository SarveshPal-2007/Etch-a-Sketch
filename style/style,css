/* Basic styling for the body to center content */
body {
    font-family: sans-serif;
    display: flex;
    flex-direction: column;
    align-items: center;
    background-color: #f0f0f0;
}

/* Style for the button */
#reset-button {
    margin: 20px;
    padding: 10px 20px;
    font-size: 16px;
    cursor: pointer;
}

/* Container for the grid squares */
#grid-container {
    width: 960px;
    height: 960px;
    display: flex;
    flex-wrap: wrap; /* Allows items to wrap to the next line */
    border: 2px solid black;
    background-color: white;
}

/* Individual grid squares */
.grid-square {
    /* box-sizing prevents border from adding to the element's size */
    box-sizing: border-box; 
    /* border: 1px solid #ddd;  Uncomment to see the grid lines */
}